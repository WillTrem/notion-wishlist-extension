{
  "name": "notion-wishlist-extension",
  "version": "1.0.0",
  "description": "This is a firefox extension to allow saving items easily to a notion wishlist.",
  "main": "index.js",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "web-ext build -o --ignore-files=\"./React Version/\"",
    "start:firefox": "web-ext run --source-dir ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "tailwindcss": "^3.3.3",
    "web-ext": "^7.8.0"
  }
}
